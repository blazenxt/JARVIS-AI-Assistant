# ==========================================
# JARVIS AI ASSISTANT - DEPENDENCIES
# Production & Cloud-Hosting Ready (Railway/Render)
# ==========================================

# 1. Voice Recognition & Audio Input
SpeechRecognition>=3.10.0
# Note: For local desktop microphone input in CLI mode, install pyaudio:
#   Windows: pip install pipwin && pipwin install pyaudio
#   Mac/Linux: pip install pyaudio

# 2. Text-to-Speech (Voice Output)
edge-tts>=6.1.9      # High-quality natural Microsoft Edge neural voices
pyttsx3>=2.90        # Offline fallback TTS engine
pygame>=2.5.2        # Audio playback for edge-tts MP3 output

# 3. AI Brain (LLM Backends)
openai>=1.12.0       # For OpenAI GPT-3.5 / GPT-4 / Compatible endpoints (Ollama)
groq>=0.4.2          # For ultra-fast Groq Llama3 / Mixtral API (Free tier available)
google-generativeai>=0.4.0  # For Google Gemini API

# 4. System & OS Automation Skills
psutil>=5.9.8        # CPU, RAM, Battery monitoring
# pyautogui>=0.9.54  # Optional: For local desktop screenshot automation (requires X11/GUI display)

# 5. Web & Search Skills
requests>=2.31.0     # Weather, news, and REST APIs
wikipedia>=1.4.0     # Wikipedia summary search
# pywhatkit>=5.4     # Optional: For local desktop YouTube playback automation (requires X11/GUI display)

# 6. Web Dashboard HUD (FastAPI / Server Utilities)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
jinja2>=3.1.3

# 7. Utilities
python-dotenv>=1.0.1 # Load API keys from .env file
